services:
  - type: web
    name: smartply-backend
    env: python
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
    nativeEnvironments:
      - ffmpeg
